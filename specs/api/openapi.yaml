openapi: 3.1.0
info:
  title: Reservation MVP API
  version: 0.1.0
  description: MVP-native reservation API.
servers:
  - url: /api/v1
security:
  - bearerAuth: []
paths:
  /auth/signup:
    post:
      operationId: signup
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignupRequest"
      responses:
        "201":
          description: Signed-up session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
        "400":
          $ref: "#/components/responses/BadRequest"
  /auth/signin:
    post:
      operationId: signin
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SigninRequest"
      responses:
        "200":
          description: Signed-in session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
        "400":
          $ref: "#/components/responses/BadRequest"
  /auth/signout:
    post:
      operationId: signout
      responses:
        "204":
          description: Signed out
  /resources:
    get:
      operationId: listResources
      security: []
      parameters:
        - $ref: "#/components/parameters/Search"
        - $ref: "#/components/parameters/Page"
      responses:
        "200":
          description: Resource page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourcePage"
  /resources/{id}:
    get:
      operationId: getResource
      security: []
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Resource detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Resource"
        "404":
          $ref: "#/components/responses/NotFound"
  /resources/{id}/availability:
    get:
      operationId: getResourceAvailability
      security: []
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: date
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Daily availability
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Availability"
  /me:
    get:
      operationId: getMe
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Me"
  /reservations:
    get:
      operationId: listOwnReservations
      responses:
        "200":
          description: Own reservations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationPage"
    post:
      operationId: createReservation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReservationCreate"
      responses:
        "201":
          description: Reservation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reservation"
        "400":
          $ref: "#/components/responses/BadRequest"
  /reservations/{id}/cancel:
    post:
      operationId: cancelReservation
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Cancelled reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reservation"
  /staff/units:
    get:
      operationId: listStaffUnits
      responses:
        "200":
          description: Staff-manageable units
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Unit"
  /staff/resources:
    get:
      operationId: listStaffResources
      responses:
        "200":
          description: Staff-manageable resource page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourcePage"
    post:
      operationId: createStaffResource
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResourceWrite"
      responses:
        "201":
          description: Created resource
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Resource"
  /staff/resources/{id}:
    patch:
      operationId: updateStaffResource
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResourcePatch"
      responses:
        "200":
          description: Updated resource
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Resource"
  /staff/reservations:
    get:
      operationId: listStaffReservations
      parameters:
        - name: unit
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Staff-visible reservations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReservationPage"
  /staff/reservations/{id}/approve:
    post:
      operationId: approveReservation
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Approved reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reservation"
  /staff/reservations/{id}/deny:
    post:
      operationId: denyReservation
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Denied reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reservation"
  /staff/reservations/{id}/cancel:
    post:
      operationId: staffCancelReservation
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: Cancelled reservation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reservation"
  /staff/memberships:
    get:
      operationId: listStaffMemberships
      parameters:
        - name: unit
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Staff memberships
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UnitStaffMembership"
    post:
      operationId: createStaffMembership
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnitStaffMembershipWrite"
      responses:
        "201":
          description: Created membership
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitStaffMembership"
  /staff/memberships/{id}:
    delete:
      operationId: deleteStaffMembership
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "204":
          description: Deleted membership
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    Id:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    Search:
      name: search
      in: query
      schema:
        type: string
    Page:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
  schemas:
    LocalizedText:
      type: object
      required: [fi, en]
      properties:
        fi:
          type: string
        en:
          type: string
    Error:
      type: object
      required: [detail]
      properties:
        detail:
          type: string
        fields:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
    Unit:
      type: object
      required: [id, name, address, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        name:
          $ref: "#/components/schemas/LocalizedText"
        address:
          $ref: "#/components/schemas/LocalizedText"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    Resource:
      type: object
      required:
        [
          id,
          unit,
          name,
          description,
          reservationForm,
          capacity,
          slotMinutes,
          requiresApproval,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: string
          format: uuid
        unit:
          $ref: "#/components/schemas/Unit"
        name:
          $ref: "#/components/schemas/LocalizedText"
        description:
          $ref: "#/components/schemas/LocalizedText"
        reservationInstructions:
          $ref: "#/components/schemas/LocalizedText"
        reservationForm:
          $ref: "#/components/schemas/ReservationForm"
        capacity:
          type: integer
          minimum: 1
        slotMinutes:
          type: integer
          enum: [15, 30, 45, 60, 90, 120]
        requiresApproval:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ResourceWrite:
      type: object
      required: [unitId, name, capacity, slotMinutes, requiresApproval]
      properties:
        unitId:
          type: string
          format: uuid
        name:
          $ref: "#/components/schemas/LocalizedText"
        description:
          $ref: "#/components/schemas/LocalizedText"
        reservationInstructions:
          $ref: "#/components/schemas/LocalizedText"
        reservationForm:
          $ref: "#/components/schemas/ReservationForm"
        capacity:
          type: integer
          minimum: 1
        slotMinutes:
          type: integer
        requiresApproval:
          type: boolean
    ResourcePatch:
      type: object
      properties:
        unitId:
          type: string
          format: uuid
        name:
          $ref: "#/components/schemas/LocalizedText"
        description:
          $ref: "#/components/schemas/LocalizedText"
        reservationInstructions:
          $ref: "#/components/schemas/LocalizedText"
        reservationForm:
          $ref: "#/components/schemas/ReservationForm"
        capacity:
          type: integer
          minimum: 1
        slotMinutes:
          type: integer
        requiresApproval:
          type: boolean
    ResourcePage:
      type: object
      required: [count, results]
      properties:
        count:
          type: integer
        results:
          type: array
          items:
            $ref: "#/components/schemas/Resource"
    AvailabilitySlot:
      type: object
      required: [start, end, available]
      properties:
        start:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
        available:
          type: boolean
    Availability:
      type: object
      required: [resourceId, date, slots]
      properties:
        resourceId:
          type: string
          format: uuid
        date:
          type: string
          format: date
        slots:
          type: array
          items:
            $ref: "#/components/schemas/AvailabilitySlot"
    ReservationState:
      type: string
      enum: [requested, confirmed, denied, cancelled]
    ReservationFormFieldKey:
      type: string
      enum: [name, phoneNumber, email, address, additionalInfo]
    ReservationFormFieldConfig:
      type: object
      required: [key, required]
      properties:
        key:
          $ref: "#/components/schemas/ReservationFormFieldKey"
        required:
          type: boolean
    ReservationForm:
      type: object
      required: [fields]
      properties:
        fields:
          type: array
          items:
            $ref: "#/components/schemas/ReservationFormFieldConfig"
    ReservationFormAnswers:
      type: object
      properties:
        name:
          type: string
        phoneNumber:
          type: string
        email:
          type: string
          format: email
        address:
          type: string
        additionalInfo:
          type: string
    Reservation:
      type: object
      required: [id, resource, user, begin, end, state, formAnswers, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        resource:
          $ref: "#/components/schemas/Resource"
        user:
          $ref: "#/components/schemas/UserProfile"
        begin:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
        state:
          $ref: "#/components/schemas/ReservationState"
        note:
          type: string
          deprecated: true
        formAnswers:
          $ref: "#/components/schemas/ReservationFormAnswers"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ReservationCreate:
      type: object
      required: [resourceId, begin, end, formAnswers]
      properties:
        resourceId:
          type: string
          format: uuid
        begin:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
        note:
          type: string
          deprecated: true
        formAnswers:
          $ref: "#/components/schemas/ReservationFormAnswers"
    ReservationPage:
      type: object
      required: [count, results]
      properties:
        count:
          type: integer
        results:
          type: array
          items:
            $ref: "#/components/schemas/Reservation"
    UserProfile:
      type: object
      required: [id, email, name, isAdmin]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        name:
          type: string
        isAdmin:
          type: boolean
    SignupRequest:
      type: object
      required: [email, name, password]
      properties:
        email:
          type: string
          format: email
          maxLength: 150
        name:
          type: string
          minLength: 1
        password:
          type: string
          format: password
          minLength: 1
    SigninRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          minLength: 1
    AuthSession:
      type: object
      required: [token, user]
      properties:
        token:
          type: string
        user:
          $ref: "#/components/schemas/UserProfile"
    Me:
      allOf:
        - $ref: "#/components/schemas/UserProfile"
        - type: object
          required: [staffUnitIds]
          properties:
            staffUnitIds:
              type: array
              items:
                type: string
                format: uuid
    UnitStaffMembership:
      type: object
      required: [id, unit, user, createdAt]
      properties:
        id:
          type: string
          format: uuid
        unit:
          $ref: "#/components/schemas/Unit"
        user:
          $ref: "#/components/schemas/UserProfile"
        createdAt:
          type: string
          format: date-time
    UnitStaffMembershipWrite:
      type: object
      required: [unitId, userId]
      properties:
        unitId:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
